<%= form_for [:admin, @department] do |d| %>
  <%= d.text_field :name, placeholder: 'Full Name' %>
  <%= d.text_field :abbrev, placeholder: 'Abbreviation' %>
  <%= d.submit 'Create Department' %>
<% end %>
